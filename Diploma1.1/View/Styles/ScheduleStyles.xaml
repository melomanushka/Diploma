<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <!-- Стиль для заголовков дней -->
    <Style x:Key="ScheduleHeaderStyle" TargetType="TextBlock">
        <Setter Property="Background" Value="#F8F9FA"/>
        <Setter Property="Padding" Value="10"/>
        <Setter Property="TextAlignment" Value="Center"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="BorderBrush" Value="#E0E0E0"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <!-- Стиль для временных слотов -->
    <Style x:Key="TimeSlotStyle" TargetType="TextBlock">
        <Setter Property="Background" Value="#F8F9FA"/>
        <Setter Property="Padding" Value="10"/>
        <Setter Property="TextAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="BorderBrush" Value="#E0E0E0"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <!-- Стиль для элементов расписания -->
    <Style x:Key="ScheduleItemStyle" TargetType="Border">
        <Setter Property="Margin" Value="2"/>
        <Setter Property="BorderBrush" Value="#E0E0E0"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="4"/>
        <Setter Property="Padding" Value="10"/>
    </Style>

    <!-- Стиль для разделительных линий -->
    <Style x:Key="GridLineStyle" TargetType="Rectangle">
        <Setter Property="Fill" Value="#E0E0E0"/>
        <Setter Property="Width" Value="1"/>
        <Setter Property="Height" Value="1"/>
    </Style>
</ResourceDictionary> 